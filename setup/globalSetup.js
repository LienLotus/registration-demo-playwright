/**
 * Global setup script for Playwright
 * Runs once before all tests - e.g. create base data or login tokens.
 */

module.exports = async (config) => {
  console.log('Global setup: preparing test environment...');
};
